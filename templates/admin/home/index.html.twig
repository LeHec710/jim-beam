{% extends 'admin/base.html.twig' %}

{% set menu = 'dashboard' %}

{% set breadcrumbs = [
    {"title": "Tableau de bord", 'url' : ""},
] %}

{% block body %}

  <div class="">
    <div class="row">

      <div class="col-md-8">
        <div class="card card-stats">
          <div class="card-header">
           <h6 class="text-light text-uppercase ls-1 mb-1">
                  Statistiques
                </h6>
                <h5 class="h3">
                  Export des données
                </h5>
          </div>
          <div class="card-body">
            <div class="row">
              <div class="col-12">

                <a download href="{{ path('export_user')}}" href="" class="btn btn-primary mb-3">
                <i class="fas fa-arrow-down mr-2"></i>
                Exporter les participants</a>
                {% if project_type != 'register' %}

                <a download href="{{ path('export_igs')}}" class="btn btn-primary mb-3">
                <i class="fas fa-arrow-down mr-2"></i>
                Exporter les gagnants</a>
                
                {% endif %}
                
              
                {# <div class="btn-group">
                  <a id="all" class="btn btn-sm btn-outline-primary">Tout</a>
                  <a id="one_month" class="btn btn-sm btn-outline-primary">1 mois</a>
                  <a id="six_months" class="btn btn-sm btn-outline-primary">6 mois</a>
                </div> #}

                {# <div style="width:100%" id="chart-timeline"></div> #}

              </div>


            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

{% endblock %}
{# {% block javascripts %}
  {{ parent() }}

<script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
<script>
  var options = {
        series: [{
          data: [
            [new Date('2023-06-24').getTime(), (Math.random() * 100).toFixed(2)],
            [new Date('2023-06-25').getTime(), (Math.random() * 100).toFixed(2)],
            [new Date('2023-06-26').getTime(), (Math.random() * 100).toFixed(2)],
            [new Date('2023-06-27').getTime(), (Math.random() * 100).toFixed(2)]
          ]
          }],
          chart: {
          id: 'area-datetime',
          type: 'area',
          height: 350,
          zoom: {
            autoScaleYaxis: true
          }
        },
        dataLabels: {
          enabled: false
        },
        markers: {
          size: 0,
          style: 'hollow',
        },
        xaxis: {
          type: 'datetime',
          min: new Date('2023-06-24').getTime(),
          tickAmount: 6,
        },
        tooltip: {
          x: {
            format: 'dd MMM yyyy'
          }
        },
        stroke: {
          curve: 'smooth'
        },
        fill: {
          type: 'gradient',
          gradient: {
            shadeIntensity: 1,
            opacityFrom: 0.7,
            opacityTo: 0.9,
            stops: [0, 100],
            gradientToColors: ['#0d6efd'],
          }
        },
    };

        var chart = new ApexCharts(document.querySelector("#chart-timeline"), options);
        chart.render();
      
  </script>
{% endblock %} #}
