<nav class="sidenav navbar navbar-vertical fixed-left navbar-expand-xs navbar-light bg-white step4" id="sidenav-main">
  <div class="scrollbar-inner">
    <div class="sidenav-header d-flex align-items-top">
      <a class="navbar-brand" href="{{path('admin_home_index')}}">
        <img style="max-height: 100%;" src="{{ asset('assets/') }}img/logo.png" class="navbar-brand-img" alt="{{project_name}}">
      </a>
      <div class="ml-auto">
        <div class="sidenav-toggler d-none d-xl-block" data-action="sidenav-unpin" data-target="#sidenav-main">
          <div class="sidenav-toggler-inner">
            <i class="sidenav-toggler-line"></i>
            <i class="sidenav-toggler-line"></i>
            <i class="sidenav-toggler-line"></i>
          </div>
        </div>
      </div>
    </div>
    <div class="navbar-inner">

      <div class="collapse navbar-collapse" id="sidenav-collapse-main">

          <ul class="navbar-nav">
            <li class="nav-item">
          <a class="nav-link {% if menu is defined and menu == "dashboard" %}active{% endif %}" href="{{path('admin_home_index')}}">
                <i class="fas fa-home text-blue"></i>
                <span class="nav-link-text">Tableau de bord</span>
              </a>
            </li>
            {% if is_granted('ROLE_SUPER_ADMIN') or is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
              <a class="nav-link {% if menu is defined and menu == "profile" %}active{% endif %}" href="{{path('profile')}}">
                <i class="fas fa-user text-blue"></i>
                <span class="nav-link-text">Mon profil</span>
              </a>
            </li>

            <li class="nav-item">
              <a class="nav-link {% if menu is defined and menu == "lottery" %}active{% endif %}" href="{{path('lottery_index')}}">
                <i class="fas fa-gift text-blue"></i>
                <span class="nav-link-text">Tirage au sort</span>
              </a>
            </li>
            {% endif %}

            {% if (is_granted('ROLE_SUPER_ADMIN') or is_granted('ROLE_USER_BACKEND')) and project_type == 'instant_win' %}

            <li class="nav-item">
              <a class="nav-link {% if menu is defined and menu == "gains" %}active{% endif %}" href="{{path('lottery_manage')}}">
                <i class="fas fa-clipboard-list text-blue"></i>
                <span class="nav-link-text">Gestion des gains</span>
              </a>
            </li>

            {% endif %}

            {% if is_granted('ROLE_SUPER_ADMIN') or is_granted('ROLE_ADMIN') %}
              <li class="nav-item">
                  <a class="nav-link {% if menu is defined and menu == "administration" %}active{% endif %}" href="#menu-administration" data-toggle="collapse" role="button" aria-expanded="{% if menu is defined and menu == "administration" %}true{% else %}false{% endif %}" aria-controls="navbar-forms">
                      <i class="fas fa-cog text-blue"></i>
                      <span class="nav-link-text">Administration</span>
                  </a>
                  <div class="collapse {% if menu is defined and menu == "administration" %}show{% endif %}" id="menu-administration">
                      <ul class="nav nav-sm flex-column">
                          <li class="nav-item">
                              <a class="nav-link {% if submenu is defined and submenu == "users" %}active{% endif %}" href="{{path('user_index')}}">
                                  <span class="sidenav-mini-icon"> U </span>
                                  <span class="nav-link-text"> Utilisateurs</span>
                              </a>
                          </li>
                      </ul>
                  </div>
              </li>
            {% endif %}

              {# {% if is_granted('ROLE_ALLOWED_TO_SWITCH') or is_granted('ROLE_PREVIOUS_ADMIN')  %}
                  <hr class="my-1">

                  <li class="nav-item">
                      {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                          <a href="{{ path('admin_home_index', {'_switch_user': '_exit'}) }}"  class="nav-link">
                              <i class="fas fa-sign-out-alt  text-blue"></i> Quitter Switch mode
                          </a>
                      {% else %}
                          <a href="{{ path("switch_home")}}" class="nav-link {% if menu is defined and menu == "switch" %}active{% endif %}">
                              <i class="fas fa-user-friends  text-blue"></i> <span class="nav-link-text">Switch</span>
                          </a>
                      {% endif %}
                  </li>
              {% endif %} #}

        </ul>

      </div>
    </div>
  </div>
</nav>
